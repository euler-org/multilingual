$globals:
  version: "8.1"

(?P<language>.+)-build:
  phony: true
  regex: true
  commands:
    - yam -d ${language}/ build

(?P<language>.+)-install:
  phony: true
  regex: true
  commands:
    - yam -d ${language}/ install

(?P<language>.+)-format:
  phony: true
  regex: true
  commands:
    - yam -d ${language}/ format

(?P<language>.+)-lint:
  phony: true
  regex: true
  commands:
    - yam -d ${language}/ lint

(?P<language>.+)-tests:
  phony: true
  regex: true
  commands:
    - yam -d ${language}/ tests
