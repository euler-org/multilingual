$globals:
  shell: bash
  version: "8.1"

(?P<language>.+)-install:
  phony: true
  regex: true
  commands:
    - yam -d ${language}/ install

install:
  phony: true
  requires:
    - python-install

(?P<language>.+)-format:
  phony: true
  regex: true
  commands:
    - yam -d ${language}/ format

format:
  phony: true
  requires:
    - python-format

(?P<language>.+)-lint:
  phony: true
  regex: true
  commands:
    - yam -d ${language}/ lint

lint:
  phony: true
  requires:
    - python-lint

(?P<language>.+)-tests:
  phony: true
  regex: true
  commands:
    - yam -d ${language}/ tests

tests:
  phony: true
  requires:
    - python-tests
